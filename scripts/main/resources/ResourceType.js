define("resources/ResourceType", [], function () {
    /**
     * @enum {number}
     */
    const ResourceType = {
        IMAGE: 0,
        SOUND: 1,
        FONT: 2,
        STRINGS: 3,
    };

    return ResourceType;
});
