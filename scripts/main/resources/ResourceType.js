define("resources/ResourceType", [], function () {
    /**
     * @enum {number}
     */
    var ResourceType = {
        IMAGE: 0,
        SOUND: 1,
        FONT: 2,
        STRINGS: 3,
    };

    return ResourceType;
});
